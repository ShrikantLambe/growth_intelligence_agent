# Full development dependencies (local Streamlit UI + data generation + API)
# For Vercel-only deployment see api/requirements.txt and pyproject.toml

# ── LLM / Agent ──────────────────────────────────────────────────────────────
anthropic>=0.25.0
openai>=1.30.0
langchain>=0.2.0
langchain-anthropic>=0.1.15
langchain-openai>=0.1.8
langchain-google-genai>=2.0.0
langchain-community>=0.2.0
langgraph>=0.2.0

# ── Vector store (local RAG) ──────────────────────────────────────────────────
faiss-cpu>=1.8.0
sentence-transformers>=3.0.0

# ── Data ─────────────────────────────────────────────────────────────────────
pandas>=2.2.0
numpy>=1.26.0
faker>=25.0.0

# ── API / serving ─────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn>=0.30.0
python-dotenv>=1.0.0
tiktoken>=0.7.0

# ── Streamlit UI (local only — not used in Vercel deployment) ─────────────────
streamlit>=1.35.0
plotly>=5.22.0
